[package]
name = "end_to_end"
version.workspace = true
edition.workspace = true

[dependencies]
parser = { path = "../parser" }
proto = { path = "../proto" }
estree = { path = "../estree" }
transformer = { path = "../transformer" }
code_generation = { path = "../code_generation" }
